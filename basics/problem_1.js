// Find Odd and Even Numbers

let number = 10; // pass number and check wether it odd or even

if (number % 2 == 0) {
    console.log(`${number} is an Even number`);
}
else {
    console.log(`${number} is an Odd number`);
}